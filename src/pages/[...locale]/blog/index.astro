---
import BaseLayout from '~/layouts/BaseLayout.astro';
import PostList from '~/components/PostList.astro';
import { SITE } from '~/config';
import { useTranslations } from '~/i18n/utils';
import { getPosts } from '~/utils/posts';

export function getStaticPaths() {
  return SITE.locales.map((l) => ({
    params: { locale: l === SITE.defaultLocale ? undefined : l },
    props: { locale: l },
  }));
}
const { locale } = Astro.props;
const t = useTranslations(locale);

const posts = await getPosts(locale, { section: 'blog' });
---

<BaseLayout
  title={t('blog.title')}
  description={t('blog.description')}
  pathWithoutLocale="/blog/"
  crumbs={[{ label: t('blog.title') }]}
>
  <header class="mb-6">
    <h1 class="text-3xl font-bold tracking-tight">{t('blog.title')}</h1>
    <p class="text-base-content/70 mt-1 text-sm">{t('blog.description')}</p>
  </header>
  {
    posts.length === 0 ? (
      <p class="text-base-content/70">{t('blog.empty')}</p>
    ) : (
      <PostList posts={posts} locale={locale} />
    )
  }
</BaseLayout>
